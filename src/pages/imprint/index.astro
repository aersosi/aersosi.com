---
import { getEntry } from 'astro:content';
import PageLayout from "@/layouts/PageLayout.astro";

const entry = await getEntry('pages', 'imprint');
const { Content } = await entry.render();
---

<PageLayout frontmatter={entry.data}>
    <Content />
</PageLayout>
