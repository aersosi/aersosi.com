---
// Fundations
import Text from "../elements/Text.astro";
// Content
const {url, title, description, projectType} = {
    url: "/projects/" + Astro.props.project.slug,
    title: Astro.props.project.data.title,
    description: Astro.props.project.data.description,
    projectType: Astro.props.project.data.tags[0]
};
---

<article class="group relative grid grid-cols-1 gap-x-12 gap-y-8 lg:grid-cols-3 lg:items-start">
    <div class="lg:col-span-2">
        <Text tag="h3"
              variant="textBase"
              class="font-medium tracking-tight text-base-900 dark:text-white">
            <div class="absolute -inset-x-4 -inset-y-4 z-0 scale-95 bg-base-500/10 dark:bg-base-600/10 opacity-0 transition group-hover:scale-100 group-hover:opacity-100 rounded-2xl duration-300">
            </div>
            <a href={url} aria-label={title}>
                <>
                    <div class="absolute -inset-x-4 -inset-y-6 z-20 sm:-inset-x-6 sm:rounded-2xl"></div>
                    <div class="relative z-10">{title}</div>
                </>
            </a>
        </Text>
        <Text tag="p" variant="textBase" class="relative z-10 mt-1 text-base-600 dark:text-base-300">
            {description}
        </Text>
    </div>

    <Text tag="p" variant="textBase"
          class="relative z-10 flex items-center text-base-600 dark:text-base-300 lg:ml-auto">
        {projectType}
    </Text>
</article>
