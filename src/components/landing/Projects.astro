---
// Fundations
import Text from "../lib/elements/Text.astro";
import Wrapper from "../lib/containers/Wrapper.astro";
import { getCollection } from "astro:content";
import ProjectEntry from "../blog/ProjectEntry.astro";
import Link from "../lib/elements/Link.astro";
import ArrowRight from "../lib/icons/ArrowRight.astro";

const allProjectsUnsorted = await getCollection("projects");
const allProjects = allProjectsUnsorted.sort(
    (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
)
---

<section>
    <Wrapper variant="standard">
        <div class="grid grid-cols-1 gap-x-12 gap-y-8 lg:grid-cols-3 py-24 border-b border-base-400 dark:border-base-600">
            <div class="lg:sticky lg:top-16 lg:z-40 self-start">
                <Text tag="h2" variant="textBase" class="font-medium text-base-900 dark:text-white pb-2">
                    Neuste Projekte
                </Text>
                <Link href="/projects" size="sm" variant="accent">
                    Alle Projekte
                    <ArrowRight size="sm"/>
                </Link>
            </div>
            <div class="lg:col-span-2 flex flex-col gap-8">
                {allProjects.slice(0, 3).map((post) =>
                        <ProjectEntry post={post}/>)}
            </div>
        </div>
    </Wrapper>
</section>
