<script is:inline>
  (function () {
    const darkStored = localStorage.getItem("darkTheme") === "true";
    const systemPrefersDark = window.matchMedia("(prefers-color-scheme: dark)").matches;
    const isDark = darkStored || (!localStorage.getItem("darkTheme") && systemPrefersDark);
    document.documentElement.classList.toggle("dark", isDark);

    function updateTheme(enableDark) {
      document.documentElement.classList.toggle("dark", enableDark);
      if (enableDark) {
        localStorage.setItem("darkTheme", "true");
      } else {
        localStorage.removeItem("darkTheme");
      }
    }

    document.addEventListener("click", (event) => {
      const toggle = event.target.closest("[data-theme-toggle]");
      if (!toggle) return;
      const currentlyDark = document.documentElement.classList.contains("dark");
      updateTheme(!currentlyDark);
    });

    // Keyboard-Shortcut
    document.addEventListener("keydown", (event) => {
      if (event.shiftKey && event.ctrlKey && event.key === "D") {
        const currentlyDark = document.documentElement.classList.contains("dark");
        updateTheme(!currentlyDark);
        event.preventDefault();
      }
    });
  })();
</script>
